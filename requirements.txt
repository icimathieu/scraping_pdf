# Dependances Python du scraping Gallica.
# Versions epinglees sur celles utilisees pour la collecte du corpus.
# Python 3.10+ recommande.

requests==2.32.5
urllib3==2.6.3
selenium==4.41.0
pdf2image==1.17.0
Pillow==12.1.1

# Dependances systeme (NON installees via pip) :
#   - Firefox (etape 2 PDF, telechargement Selenium)
#   - geckodriver (chemin par defaut : /opt/homebrew/bin/geckodriver sur macOS Apple Silicon)
#   - poppler (etape 3 PDF, binaires pdfinfo / pdftoppm)
#       macOS  : brew install poppler
#       Linux  : apt install poppler-utils
